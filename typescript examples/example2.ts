const personDescription1 = (name: string, city: string , age: number) => 
    `${name} lives in ${city}, his age is ${age}. In 10 years he will be ${age+10}`;

console.log(personDescription1("Kostas","Athens",20));
